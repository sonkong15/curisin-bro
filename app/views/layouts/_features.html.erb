<div class=" box">

	<% @posts.each do |p| %>
	<div class="thumbnail">
	<table>
		<tr>
			<td>
				<%= link_to image_tag(p.picture.url(:small), :title => p.title, :alt => p.title, :class => "home-pictures"), posts_url(p) %>
			</tb>

		</tr>
		<tr>
			<td class = "home-title">
				<%= link_to  p.title,  posts_url(p) %>
			</tb>

		</tr>


	</table>
	</div>
	<% end %>
	</div>
